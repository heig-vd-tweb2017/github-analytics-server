# server

Pour utiliser le projet il est nécessaire de créer le fichier <github-credentials.json> contenant le username et token.
Ce fichier est pas défaut inclus dans le .gitignore pour des raisons de sécurité.
{
    "username": "nom"
    "token":"tocken généré sur github"
}